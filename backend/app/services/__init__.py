"""
services/ — reusable business-logic modules for TickerTap.

Each module is a pure-Python service with no FastAPI dependencies,
making them independently testable and importable by multiple routes.
"""
