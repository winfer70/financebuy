# trading-ml/requirements.txt — Python dependencies for the ML container.
#
# Kept minimal: PyTorch (CPU only), scikit-learn for classical ML,
# hmmlearn for regime detection, pandas/numpy for data wrangling,
# joblib for model persistence, FastAPI/uvicorn for the internal API.
torch>=2.0,<3.0
--extra-index-url https://download.pytorch.org/whl/cpu
scikit-learn>=1.3
hmmlearn>=0.3
pandas>=2.0
numpy>=1.24
joblib>=1.3
fastapi>=0.100
uvicorn[standard]>=0.22
pydantic>=2.0
